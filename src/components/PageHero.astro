---
const { eyebrow, title, description, className = "" } = Astro.props;
---

<section class:list={["page-hero", className]}>
  <div class="shell page-hero-inner">
    <p class="eyebrow">{eyebrow}</p>
    <h1>{title}</h1>
    <p class="page-intro">{description}</p>
  </div>
</section>
